'use strict';

(function() {

  const cronos = modulejs.require('cronos');
  const ui = modulejs.require('ui');
  const map = modulejs.require('map');

  cronos.init();
  ui.init();
  map.init();

})()

